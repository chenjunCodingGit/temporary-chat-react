/**
 * Add a unified prefix to the class name
 * @param className - Original class name
 * @returns {string} Class name with prefix added
 */
export declare const withPrefix: (className: string) => string;
